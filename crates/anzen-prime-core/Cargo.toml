[package]
name = "anzen-prime-core"
version = "0.1.0"
edition = "2021"
license = "MIT"

[dependencies]
anzen-cold-signer = { path = "../../vendor/anzen-cold-signer" }
k256 = { version = "0.13.4", default-features = false, features = ["schnorr"] }
sha2 = "0.10"
