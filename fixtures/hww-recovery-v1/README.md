# HWW recovery v1 development snapshot

This synthetic regtest fixture represents the chain facts Luke's current
`hww recover` command reads directly: a descriptor, destination, tip height,
and observed vault UTXO. It is test plumbing for the Prime review/signing path,
not a proposed phone or chain transport.
